# Copy this file to .env (same directory as docker-compose.yml) and fill
# in real values. Compose reads .env automatically - anything left unset
# here falls back to the placeholder default in docker-compose.yml itself.
#
# .env should NOT be committed anywhere public - it holds real credentials.
#
# IMPORTANT: Compose applies its own ${VAR} interpolation syntax to THIS
# FILE's values too, not just docker-compose.yml. A raw, unescaped `$` in
# any value here (e.g. a randomly generated password that happens to
# contain one) gets treated as the start of a variable reference -
# Compose silently substitutes an empty string for whatever follows if
# no such variable exists, truncating the value with no error or
# warning. If you must include a literal `$`, escape it as `$$`.
# Simplest fix: generate every secret below with `openssl rand -hex N` -
# hex output can never contain `$` or any other character Compose treats
# specially, so this whole class of bug is structurally impossible.

### Parser images (once pushed to your own Docker Hub) ###
PARSER_COLMI_IMAGE=dibz15/colmi2influx:latest
# Best-effort/unverified against real hardware - see
# parser/activefit/README.md. Safe to run continuously before pairing
# the watch (gracefully no-ops), but don't trust the data it produces
# as correct until you've gone through that README's checklist.
PARSER_ACTIVEFIT_IMAGE=dibz15/activefit2influx:latest

### wearable-events image (once pushed to your own Docker Hub) ###
WEARABLE_EVENTS_IMAGE=dibz15/wearable-events:latest

### Storage root - all persistent volumes live under here ###
APPDATA_ROOT=/DATA/AppData/biomarker-stack

### Shared InfluxDB settings (used by both the influxdb and parser services) ###
INFLUXDB_ORG=home
INFLUXDB_BUCKET=health
# Generate with `openssl rand -hex 32` - it's a real credential with
# full API access to your health data, not a password to remember.
INFLUXDB_TOKEN=changeme_admin_token
INFLUXDB_MEASUREMENT=colmi
INFLUXDB_INIT_USERNAME=admin
# Generate with `openssl rand -hex 24` (see the note at the top of this
# file about why hex output specifically, not an arbitrary password).
INFLUXDB_INIT_PASSWORD=changeme_admin_password

### Grafana ###
GRAFANA_ADMIN_USER=admin
# Generate with `openssl rand -hex 24`.
GRAFANA_ADMIN_PASSWORD=changeme_grafana_password

# Which person's HRV the provisioned "HRV dropped below baseline" alert
# rule watches - should match GADGETBRIDGE_USER for that person. See
# grafana/provisioning/alerting/rules.yaml.
ALERT_HRV_USER=primary

### ntfy ###
TZ=Europe/London

# ntfy is locked down (no open signup, deny-all default access) - see
# the ntfy setup step in README.md for the `ntfy user add` / `ntfy
# access` commands needed before either you or Grafana can publish.
# NTFY_TOPIC is what you'll subscribe to from the ntfy phone app.
NTFY_TOPIC=biomarker-alerts
# Dedicated ntfy account for Grafana to authenticate as when publishing
# alerts (separate from your own personal ntfy login, if you create one).
NTFY_GRAFANA_USER=grafana
# Generate with `openssl rand -hex 24`.
NTFY_GRAFANA_PASSWORD=changeme_ntfy_password
# ntfy priority scale: 1=min, 3=default, 5=urgent (bypasses phone
# silent/DND on most clients).
NTFY_PRIORITY=4

### Parser: Nextcloud WebDAV source ###
WEBDAV_URL=https://your-nextcloud-domain/remote.php/dav/
WEBDAV_PATH=files/your_nextcloud_user/GadgetBridge/
WEBDAV_USER=your_nextcloud_user
WEBDAV_PASS=your_nextcloud_app_password
EXPORT_FILENAME=Gadgetbridge

### Parser: behaviour ###
# Only matters on the very first run, before any checkpoint exists in
# InfluxDB - after that, sync resumes from the last checkpoint instead
# (see the "Checkpointed sync" section in the parser's own README).
QUERY_DURATION=86400
# Safety cap (seconds) on catch-up distance if the checkpoint turns out
# to be very old (e.g. the container was down for a while). Without
# this, a corrupted/ancient checkpoint could trigger an unbounded
# historical resync. Raise it if you need to backfill further after
# genuinely longer downtime.
MAX_CATCHUP_SECONDS=2592000
# Small overlap (seconds) subtracted from the checkpoint before
# resuming, so a sample landing exactly at the boundary can't be
# missed. Re-querying a few already-processed rows is harmless.
CHECKPOINT_OVERLAP_SECONDS=300
# How far ahead of "now" a sample/checkpoint can be before it's treated
# as corrupted rather than genuine (real sensors can't record data
# before it happens). See the parser's own README for why this exists
# - a single bad-timestamped sample was found to permanently break
# checkpoint-based sync otherwise.
MAX_FUTURE_TOLERANCE_SECONDS=300
SLEEP_HOURS=0,1,2,3,4,5,6
# Confirmed via real R09 hardware data (Aug 2026) that Gadgetbridge's
# COLMI_* tables store TIMESTAMP in milliseconds, not seconds - the
# opposite of what earlier untested guessing assumed. Y is now the
# verified-correct default for this device family; only flip to N if
# your own export's timestamps land implausibly far in the future in
# Grafana (a sign your build/model differs).
COLMI_TIMESTAMPS_ARE_MS=Y
REMOVE_TEMP_DB=Y
SYNC_INTERVAL_SECONDS=1800

# Who this data belongs to. Tags every InfluxDB point. Defaults to
# "primary" and doesn't need to change for a single-user setup - it's a
# stop-over for if a second person/ring is ever added later (run a second
# parser instance pointed at their own WebDAV export with a different
# GADGETBRIDGE_USER value, both writing into the same bucket).
GADGETBRIDGE_USER=primary

### Wearable Events (calendar tagging + subjective sleep score) ###
EVENTS_MEASUREMENT=events
SLEEP_MEASUREMENT=subjective_sleep
CALENDAR_SYNC_INTERVAL_MINUTES=15
# Minimum sleep session length (seconds) to be eligible as "last night's
# sleep" when resolving a /sleep submission - excludes naps. 10800 = 3h.
MIN_SLEEP_SESSION_SECONDS=10800

# First-run login bootstrap. Only used if the users table is empty -
# creates this one account, then further household members are added
# from the app's Manage tab (no public signup page). Use the same
# username you use for GADGETBRIDGE_USER on your own ring parser
# instance, so your calendar/sleep data and HR/HRV data correlate later.
WEARABLE_EVENTS_ADMIN_USERNAME=primary
# Generate with `openssl rand -hex 24` (or pick a memorable passphrase if
# you'd rather type this one by hand day-to-day - it's just a login, not
# an API credential like the others in this file).
WEARABLE_EVENTS_ADMIN_PASSWORD=changeme_login_password
